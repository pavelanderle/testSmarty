Použij daný projekt a vytvoř šablonu, která zobrazí získané pole jako tabulku. V tabulce najděte slovo jako v osmisměrce a vypište jej.
Použij daný projekt a vytvoř šablonu, která vypíše všechny sedmiznaková slova (sloupce, řádky, diagonály - vždy jen v jednom směru). 
Jedno slovo bude vaše hodnocení.
